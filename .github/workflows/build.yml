name: FACM Windows Build

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: facm-windows-build-${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  SOLUTION: FACM.sln
  OUTPUT_EXE: src\FACM\bin\Release\net48\FACM.exe
  PETHOST_PROJECT: src\FACM.PetHost\FACM.PetHost.csproj
  PACKAGE_NAME: FACM-Windows-x64

jobs:
  build:
    name: Build FACM 3.5 lightweight + validate PetHost
    runs-on: windows-latest
    timeout-minutes: 25

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Set up .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Verify tool inputs
        shell: pwsh
        run: |
          $manifestPath = Join-Path $PWD 'tools/EXTRACTED-TOOLS.json'
          if (-not (Test-Path $manifestPath)) {
            throw "Missing tool manifest: $manifestPath"
          }

          $manifest = Get-Content $manifestPath -Raw -Encoding utf8 | ConvertFrom-Json
          if (-not $manifest.files -or $manifest.files.Count -lt 1) {
            throw 'Tool manifest contains no files.'
          }

          foreach ($entry in $manifest.files) {
            $path = Join-Path $PWD ('tools/' + [string]$entry.name)
            if (-not (Test-Path -LiteralPath $path -PathType Leaf)) {
              throw "Missing tool input: $($entry.name)"
            }

            $item = Get-Item -LiteralPath $path
            if ($item.Length -ne [int64]$entry.size) {
              throw "Size mismatch for $($entry.name): expected $($entry.size), got $($item.Length)"
            }

            $actual = (Get-FileHash -LiteralPath $path -Algorithm SHA256).Hash.ToLowerInvariant()
            $expected = ([string]$entry.sha256).ToLowerInvariant()
            if ($actual -ne $expected) {
              throw "SHA256 mismatch for $($entry.name): expected $expected, got $actual"
            }

            Write-Host "Verified $($entry.name): $actual"
          }

      - name: Report cleanup configuration state
        shell: pwsh
        run: |
          $profilePath = 'src/FACM/Configuration/CleanupProfile.cs'
          $profile = Get-Content $profilePath -Raw -ErrorAction SilentlyContinue
          if (-not $profile) {
            throw "CleanupProfile.cs not found at expected path: $profilePath"
          }
          elseif ($profile -match 'REPLACE_[A-Z0-9_]+') {
            Write-Warning 'CleanupProfile.cs still contains REPLACE_ placeholders. FACM will build, but cleanup stays disabled until the developer fills them in.'
          }
          else {
            Write-Host 'CleanupProfile.cs contains no REPLACE_ placeholders.'
          }

      - name: Build and self-test optional VPet PetHost
        shell: pwsh
        run: |
          dotnet build $env:PETHOST_PROJECT -c Release
          if ($LASTEXITCODE -ne 0) {
            throw "PetHost build failed with exit code $LASTEXITCODE"
          }

          $hostDll = Get-ChildItem -Path 'src\FACM.PetHost\bin\Release' -Filter 'FACM.PetHost.dll' -Recurse |
            Select-Object -First 1
          if (-not $hostDll) {
            throw 'PetHost build output FACM.PetHost.dll was not found.'
          }

          $selfTestRoot = Join-Path $env:RUNNER_TEMP 'FACM-PetHost-SelfTest'
          if (Test-Path $selfTestRoot) {
            Remove-Item $selfTestRoot -Recurse -Force
          }
          New-Item -ItemType Directory -Path $selfTestRoot -Force | Out-Null

          & dotnet $hostDll.FullName --self-test --data-root $selfTestRoot
          if ($LASTEXITCODE -ne 0) {
            $errorFile = Join-Path $selfTestRoot 'self-test-error.txt'
            if (Test-Path $errorFile) {
              Get-Content $errorFile -Raw | Write-Host
            }
            throw "PetHost self-test failed with exit code $LASTEXITCODE"
          }

          Write-Host 'Optional PetHost build/self-test passed. It is intentionally not embedded in the FACM 3.5 portable EXE.'

      - name: Restore and build lightweight Release
        shell: pwsh
        run: |
          if (Test-Path 'out\PetHostBundle.zip') {
            Remove-Item 'out\PetHostBundle.zip' -Force
          }

          msbuild $env:SOLUTION /restore /m /p:Configuration=Release /p:Platform="Any CPU" /p:ContinuousIntegrationBuild=true /v:minimal
          if ($LASTEXITCODE -ne 0) {
            throw "MSBuild failed with exit code $LASTEXITCODE"
          }
          if (-not (Test-Path $env:OUTPUT_EXE -PathType Leaf)) {
            throw "Build output not found: $env:OUTPUT_EXE"
          }

      - name: Verify lightweight FACM.exe
        id: verify
        shell: pwsh
        run: |
          $exe = (Resolve-Path $env:OUTPUT_EXE).Path
          $bytes = [IO.File]::ReadAllBytes($exe)
          if ($bytes.Length -lt 2 -or $bytes[0] -ne 0x4D -or $bytes[1] -ne 0x5A) {
            throw 'Build output is not a valid Windows PE executable.'
          }

          $version = [Diagnostics.FileVersionInfo]::GetVersionInfo($exe)
          if ([string]::IsNullOrWhiteSpace($version.FileVersion)) {
            throw 'FACM.exe has no FileVersion.'
          }
          if ($version.ProductName -ne 'FACM') {
            throw "Unexpected product name: $($version.ProductName)"
          }
          if ($version.FileVersion -ne '3.5.18.0') {
            throw "Unexpected FACM version: $($version.FileVersion)"
          }

          $resourceVerifier = Join-Path $env:RUNNER_TEMP 'verify-facm-resources.ps1'
          @'
          param([Parameter(Mandatory=$true)][string]$ExePath)
          $ErrorActionPreference = 'Stop'
          $assembly = [Reflection.Assembly]::ReflectionOnlyLoadFrom($ExePath)
          $assembly.GetManifestResourceNames()
          '@ | Set-Content -LiteralPath $resourceVerifier -Encoding utf8

          $windowsPowerShell = Join-Path $env:SystemRoot 'System32\WindowsPowerShell\v1.0\powershell.exe'
          $resources = @(
            & $windowsPowerShell `
              -NoLogo `
              -NoProfile `
              -NonInteractive `
              -ExecutionPolicy Bypass `
              -File $resourceVerifier `
              -ExePath $exe
          )
          if ($LASTEXITCODE -ne 0) {
            throw "Resource verification failed with exit code $LASTEXITCODE"
          }
          if ($resources -notcontains 'FACM.Resources.FACM.ToolBundle.dll') {
            throw 'FACM.ToolBundle.dll was not embedded into FACM.exe.'
          }
          if ($resources -contains 'FACM.Resources.PetHost.zip') {
            throw 'Lightweight FACM 3.5 build unexpectedly embedded the large self-contained PetHost bundle.'
          }
          if ($bytes.Length -gt 10MB) {
            throw "FACM 3.5 portable EXE size regression: $($bytes.Length) bytes"
          }

          "file_version=$($version.FileVersion)" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "product_version=$($version.ProductVersion)" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          "size_bytes=$($bytes.Length)" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          Write-Host "Built lightweight FACM.exe version $($version.FileVersion), size $($bytes.Length) bytes"
          Write-Host 'Verified embedded ToolBundle and confirmed PetHost runtime is not embedded.'

      - name: Sign when certificate secrets exist
        shell: pwsh
        env:
          FACM_PFX_BASE64: ${{ secrets.FACM_PFX_BASE64 }}
          FACM_PFX_PASSWORD: ${{ secrets.FACM_PFX_PASSWORD }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:FACM_PFX_BASE64)) {
            Write-Host 'No FACM_PFX_BASE64 secret configured. Skipping code signing.'
            exit 0
          }
          if ([string]::IsNullOrWhiteSpace($env:FACM_PFX_PASSWORD)) {
            throw 'FACM_PFX_PASSWORD is required when FACM_PFX_BASE64 is configured.'
          }

          & .\scripts\sign-ci.ps1 `
            -ExePath $env:OUTPUT_EXE `
            -PfxBase64 $env:FACM_PFX_BASE64 `
            -PfxPassword $env:FACM_PFX_PASSWORD

      - name: Create downloadable package
        shell: pwsh
        run: |
          $artifactDir = Join-Path $PWD 'artifacts'
          if (Test-Path $artifactDir) {
            Remove-Item $artifactDir -Recurse -Force
          }
          New-Item -ItemType Directory -Path $artifactDir | Out-Null

          $output = Join-Path $artifactDir 'FACM.exe'
          Copy-Item $env:OUTPUT_EXE $output -Force

          $hash = Get-FileHash $output -Algorithm SHA256
          @(
            "$($hash.Hash) *FACM.exe"
          ) | Set-Content (Join-Path $artifactDir 'SHA256.txt') -Encoding ascii

          $signature = Get-AuthenticodeSignature $output
          $signature | Format-List Status,StatusMessage,SignerCertificate,TimeStamperCertificate |
            Out-String | Set-Content (Join-Path $artifactDir 'SIGNATURE.txt') -Encoding utf8

          $fileVersion = [Diagnostics.FileVersionInfo]::GetVersionInfo($output)
          $buildInfo = [ordered]@{
            file_version = $fileVersion.FileVersion
            product_version = $fileVersion.ProductVersion
            size_bytes = (Get-Item $output).Length
            sha256 = $hash.Hash
            signature_status = [string]$signature.Status
            signature_subject = if ($signature.SignerCertificate) { $signature.SignerCertificate.Subject } else { '' }
            signature_thumbprint = if ($signature.SignerCertificate) { $signature.SignerCertificate.Thumbprint } else { '' }
            tool_bundle = 'embedded FACM.ToolBundle.dll'
            pet_host = 'optional compatibility runtime; validated in CI but not embedded in FACM 3.5 portable EXE'
            startup = 'offline-first; update check begins after shell is visible'
            built_at_utc = [DateTime]::UtcNow.ToString('o')
            commit = '${{ github.sha }}'
          }
          $buildInfo | ConvertTo-Json | Set-Content (Join-Path $artifactDir 'BUILD-INFO.json') -Encoding utf8

          Copy-Item README.md (Join-Path $artifactDir 'README.md')
          if (Test-Path docs\DEVELOPER-CLEANUP-CONFIG.md) {
            Copy-Item docs\DEVELOPER-CLEANUP-CONFIG.md (Join-Path $artifactDir 'DEVELOPER-CLEANUP-CONFIG.md')
          }
          if (Test-Path docs\SIGNING.md) {
            Copy-Item docs\SIGNING.md (Join-Path $artifactDir 'SIGNING.md')
          }
          if (Test-Path docs\ONLINE-MANAGEMENT.md) {
            Copy-Item docs\ONLINE-MANAGEMENT.md (Join-Path $artifactDir 'ONLINE-MANAGEMENT.md')
          }
          if (Test-Path docs\VPET-PETHOST.md) {
            Copy-Item docs\VPET-PETHOST.md (Join-Path $artifactDir 'VPET-PETHOST.md')
          }

          $zip = Join-Path $PWD ($env:PACKAGE_NAME + '.zip')
          if (Test-Path $zip) {
            Remove-Item $zip -Force
          }
          Compress-Archive -Path "$artifactDir\*" -DestinationPath $zip -Force
          Write-Host "Package created: $zip"

      - name: Upload FACM package
        uses: actions/upload-artifact@v4
        with:
          name: FACM-Windows-x64-${{ github.run_number }}
          path: |
            FACM-Windows-x64.zip
            artifacts/FACM.exe
            artifacts/SHA256.txt
            artifacts/SIGNATURE.txt
            artifacts/BUILD-INFO.json
