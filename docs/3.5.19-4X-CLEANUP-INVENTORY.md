# FACM 3.5.19 / 4.x 清理清单

日期：2026-09-05  
主产品线：FACM 3.5.19（WinForms / net48 / 轻量单 EXE）  
参考产品线：FACM 4.x（只作为最佳实践来源，完成后退出默认工作树）  
关联：Draft PR #241、`docs/3.5.19-4.0.6-BACKPORT-AUDIT.md`、`docs/3.5.19-P1-BACKPORT-STATUS-2026-09-05.md`

## 1. 清理原则

在 3.5.19 回灌通过实机 Gate 后，机械删除默认工作树中的 4.x-only 项目、构建/迁移链、探针、证据和历史操作文档。

“删除 4.x”首先指删除当前工作树的 4.x-only 文件，不重写 Git 历史。Release、tag、远端临时 branch 属于第二层破坏性清理，必须等 3.5.19 主线稳定后用单独显式列表执行。

## 2. 当前门禁

#241 的功能代码在 `76337216da209fb66b45fdbca86fc39ebeeaddd1` 已验证：Windows Build run 1522 PASS、UI Text Contract run 630 PASS、轻量 EXE 资源/体积 gate PASS。之后只维护 audit/status/cleanup 文档。

Windows 10 实机 League / Mayhem / 自动隐藏 / 导航文字残影 / 主控裁剪残影仍待人工回归，所以目前**不执行 4.x 物理删除，也不合并 #241**。

## 3. RETAIN

- `FACM.sln`
- `src/FACM/`
- `src/FACM.ToolBundle/`
- `src/FACM.Updater/`
- `src/FACM.PetHost/` — 3.5 可选 VPet runtime 源码和 self-test 仍使用；#241 的 InGame `hide/show` IPC 也在这里。
- `online/version.json`
- `online/announcement.json`
- `online/mirrors.json`（当前先保留）
- `.github/workflows/build.yml`
- `.github/workflows/publish-3.5-lightweight.yml`
- `.github/workflows/manage-online.yml`
- `.github/workflows/mayhem-source-probe.yml`
- `.github/workflows/ui-text-contract.yml`
- `scripts/build-release.ps1`
- 当前 ToolBundle / updater / sign / setup helpers
- `AGENTS.md`
- `docs/3.5.19-4.0.6-BACKPORT-AUDIT.md`
- `docs/3.5.19-P1-BACKPORT-STATUS-2026-09-05.md`

`FACM.PetHost` **保留源码，但普通 3.5 EXE 不内嵌 self-contained bundle**。`FACM.csproj` 已默认 `IncludePetHostBundle=false`，只有显式 `IncludePetHostBundle=true` 或历史兼容 `RequirePetHostBundle=true` 才允许嵌入。`scripts/build-release.ps1` 也已改为只 build/self-test PetHost 并验证 FACM.exe <10 MiB、无 `FACM.Resources.PetHost.zip`。

## 4. DELETE AFTER GATE — source / solution

- `FACM4.sln`
- `src/FACM.App/`
- `src/FACM.Core/`
- `src/FACM.Infrastructure/`
- `src/FACM.Platform.Windows/`
- `src/FACM.FoundationSmoke/`
- `src/FACM.WindowsSmoke/`
- `src/FACM.FlyingHost/`
- `src/FACM.Bootstrapper/`
- `prototypes/FACM.WinUI.DeploymentProbe/`

`FACM.FlyingHost` 是 4.x bundle 路线；3.5 飞行动物不依赖该 project。

## 5. DELETE AFTER GATE — workflows / release

- `.github/workflows/facm4-foundation.yml`
- `.github/workflows/winui-deployment-probe.yml`
- `.github/workflows/finalize-3.5.18.yml`
- `.github/workflows/publish-release.yml`
- `release/request.json`

特别说明：旧 `publish-release.yml` 此前被误判为 RETAIN。实际代码明确 publish self-contained PetHost、压 `PetHostBundle.zip` 并以 `RequirePetHostBundle=true` 构建 embedded-PetHost 重型 EXE，和 `publish-3.5-lightweight.yml` 的 future canonical contract 冲突，因此必须在 cleanup PR 删除。其配套 `release/request.json` 仍是历史 3.5.15 请求，也一并退出。

若以后需要文件驱动的 lightweight 发布，只使用专门的 3.5 lightweight request，不复活旧 heavy publisher。

## 6. DELETE AFTER GATE — scripts / tools

- 全部 `scripts/check-facm4-*.ps1`
- `scripts/collect-facm4-real-machine-evidence.ps1`
- `scripts/release/Build-Facm4SelfSignedBundle.ps1`
- `scripts/release/New-Facm4SelfSignedManifestKey.ps1`
- `scripts/release/publish-release-local.ps1`
- `scripts/release/publish-gitee-release-local.ps1`
- `tools/boot1/`
- `tools/release/`

删除 `tools/boot1` / `tools/release` 前最后扫描 retained 3.5 workflow/helper 是否引用其中个别通用脚本；如有，先迁移最小 helper，再删整目录。

`tools/EXTRACTED-TOOLS.json` 及其真实输入保留，因为 `FACM.ToolBundle` 仍依赖。

## 7. DELETE AFTER GATE — online / evidence / docs

- `online/facm4-version.json`
- `FACM-4.0-真机证据采集.bat`
- `evidence/facm4-win10-*`
- `docs/FACM4-*.md`
- `docs/FACM-4.0-*.md`
- `docs/FACM-4-MIGRATION-CONTRACT.md`
- `docs/BOOT3A-TRUST.md`
- `docs/BOOT3B-KEY-GOVERNANCE.md`
- `docs/BOOT3B-SIGNED-ARTIFACTS.md`
- `docs/BOOT3C-RELEASE-READINESS.md`
- `docs/FREE-DIST-1.md`

删除历史 docs 前确保仍有价值的 4.x 结论已经吸收到 backport audit / 3.5 canonical docs。

3.5.19 发布时 `online/version.json.migration.enabled` 必须保持 false，不再指向 4.x。

## 8. MIGRATE / CONSOLIDATE

以下文件保留职责但必须重写为 3.5.19 / GGman current truth：

- `README.md` — 当前仍写 FACM 3.4.2。
- `docs/PROJECT_STATE.md` — 当前顶部仍把 3.5.18 bridge → 4.0.6 modular 写成 current line。
- `docs/ARCHITECTURE.md`
- `docs/DECISIONS.md`
- `docs/OPERATIONS.md`
- `docs/PITFALLS.md`

清理后只保留对 3.5.19 仍有效的架构、运行、故障经验；4.x 专属开发时间线交给 Git 历史。

## 9. 清理前人工 Gate

1. 连续打开/关闭紧凑主控面板，无用户视频中的横条/旧面板残影。
2. 导航按钮在静止、刷新、覆盖恢复、100/125/150/200% DPI 下无文字重影。
3. Lobby 自动搜索、ReadyCheck 自动接受实机更快且无重复动作。
4. 真网络失败可恢复；已进入队列时不重复 POST。
5. ChampSelect Mayhem 百分比正确、长内容完整、加载速度保持 3.5 优势。
6. InGame 默认球/Sprite/VPet 隐藏，PostGame/Lobby ownership 恢复正确。

## 10. 真正删除顺序

1. 完成 #241 实机 Gate；失败继续修 3.5.19，不删 4.x。
2. 合并/收尾 #241，确认 `main` lightweight build 绿色。
3. **另起 cleanup branch/PR**。
4. 先删 4.x workflows/probes/scripts 和旧 heavy `publish-release.yml`。
5. 再删 `FACM4.sln`、4.x-only projects、Bootstrapper、FlyingHost、WinUI prototype。
6. 删除 `online/facm4-version.json`、4.x evidence/root BAT/历史 docs、旧 `release/request.json`。
7. 重写 README / PROJECT_STATE / ARCHITECTURE / OPERATIONS 等 canonical 文档。
8. 全树搜索 `FACM4.sln`、`FACM.App`、`FACM.Core`、`FACM.Infrastructure`、`FACM.Platform.Windows`、`FACM.Bootstrapper`、`FACM.FlyingHost`、`facm4-version.json`、`BOOT3`、`FACM 4.0` 并逐项解释残留。
9. 重跑 Windows Build、UI Text Contract、Mayhem source probe、lightweight release-reference gate。
10. cleanup PR 全绿后合并；最后再单独枚举并处理远端 4.x Releases/tags/temp branches。

## 11. 目标工作树

```text
FACM.sln
src/FACM/
src/FACM.ToolBundle/
src/FACM.Updater/
src/FACM.PetHost/
online/version.json
online/announcement.json
.github/workflows/<3.5 current workflows>
scripts/<3.5 current helpers>
docs/<3.5 canonical docs + backport decision record>
```

默认工作树不再同时展示 FACM4/WinUI/Morphing/Core/Infrastructure/Platform.Windows/native bootstrapper/CAB/4.x gates/evidence 或旧重型 embedded-PetHost publisher。