# FACM 当前项目状态

> 2026-08-11：FACM 3.1.1 仍是当前正式版；海斗完整基础 ARAM Buff/Debuff 已完成开发同步，未在本任务中发布新正式版本。

## 当前正式版

- 版本：FACM 3.1.1。
- GitHub Release：v3.1.1。
- 在线更新：已启用。
- `minimum_version=3.0.0`。
- `force_update=false`。
- 3.1.1 发布基础 main：`e53c45773b224e4d8f670f44381e394457fdf660`。
- 3.1.1 发布元数据提交：`04f7cbae702d6dd136ab278f72938cff2a8c26ef`。
- 3.1.1 在线更新启用提交：`de632e2832e6d227aa570082601b33ed8f99a0b9`。

## 已验证完成

- FACM 3.1 的发布前稳定性收口已完成：海斗多源容灾、当前平衡版本校验、控制中心首帧布局、桌宠 outside-click、PetHost 生命周期/流畅性、图片热缓存和清理流程后台化均已进入正式链路。
- 正式 Release 流程已完成签名、SHA-256、PetHost self-test/内嵌验证、disabled manifest → Release → enabled manifest 的事务式发布验证。
- 自签名证书 GitHub Secrets 已更新，普通 Build 与正式 Release 统一使用 `FACM_PFX_BASE64` / `FACM_PFX_PASSWORD`。
- 3.1.0 已正式发布；随后发布 3.1.1 作为纯在线更新验证版。
- 用户已在真实 Windows 环境中确认：现有 3.1.0 客户端能够自动检测、下载、校验、替换并重新启动到 3.1.1，在线更新链路实机验证成功。
- Issue #28（3.1.0 正式发布）与 Issue #31（3.1.1 在线更新验证）均已完成关闭。
- 2026-08-11，PR #36 将英雄**完整基础 ARAM 当前平衡状态**接入海斗查询，并与 Mayhem 专属状态分层展示；基础状态来自 OP.GG 普通 ARAM 页面，Patch 不一致时隐藏旧完整数值，来源不可用时明确降级而不冒充“无修正”。
- 接口项目实机验收随后暴露 OP.GG 真实 HTML 兼容差异：版本可能使用 `Ver:` / `Version:`，正负号和数值可能被空白拆成 `+ 2.5%`。本任务 PR #37 将同一兼容规则同步回 FACM，并新增亚索、库奇、萨勒芬妮三类离线回归形态。
- PR #37 的 FACM Windows Build #691 与 FACM Mayhem Source Probe #37 均通过；live probe 仍保持 advisory，不作为普通核心构建的外部网络硬门禁。

## 当前阶段

FACM 当前主阶段仍标记为 **基本完成 / 可暂停**。现有 3.1.1 仍是稳定正式版；本次海斗完整基础 ARAM 改动属于 `main` 后续开发成果，是否发布新版本另行决定。

新任务仍按 `AGENTS.md`：从最新 `main` 开始，一任务一短分支 + PR；不要从旧本地快照或已合并任务分支继续开发。

## 下一阶段：轻量蜘蛛桌宠

跟踪：Issue #33 `整理轻量蜘蛛桌宠方案`。

当前只确认产品方向，不开始实现：

- 一个轻量的蜘蛛桌宠；
- 在屏幕范围内进行无规律但自然的爬行；
- 爬行速度可控；
- 尽量低 CPU / 内存占用，不影响现有 FACM 与 PetHost 稳定性；
- 过去蜘蛛方案已多次尝试但效果失败，因此新一轮必须先复盘失败模式，再确定运动模型、渲染/动画方式和交互边界；
- 方案确认前不改正式桌宠业务代码。

下一聊天若回到蜘蛛任务，继续从 Issue #33：先整理“蜘蛛应该怎么动、为什么旧方案失败、怎样做最小可验证原型”，再决定是否实现。
