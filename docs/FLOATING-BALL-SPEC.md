# FACM 2.1 悬浮球交互规格

- 空闲状态只显示 64×64 悬浮球，不创建传统主窗口。
- 悬浮球始终置顶、不会出现在任务栏，支持托盘恢复。
- 鼠标拖动时跟随光标，松手后吸附当前屏幕最近的左右边缘。
- 单击展开 326×438 的紧凑菜单，菜单显示在悬浮球内侧并自动限制在工作区内。
- 菜单失去焦点自动关闭；系统选择器、确认框和工具子菜单显示期间不误关。
- 启动使用 asInvoker，避免悬浮球每次启动都弹 UAC。
