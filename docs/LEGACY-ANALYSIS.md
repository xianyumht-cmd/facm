# 旧版 FACM.exe 静态检查记录

检查对象：用户提供的 `FACM.exe`

- SHA-256：`c5d068a2706e8dd5cf3e1ea702012e77b5102c06494c0f1d8983e4e1fb517e60`
- 文件大小：17,081,856 字节
- 格式：PE32+、x86-64、Windows GUI
- PE 时间戳：2025-12-10 16:48:55
- Authenticode Security Directory：空，旧版没有可验证的 Authenticode 签名
- `.rsrc` 资源节约 16 MB，占文件主体

静态字符串与资源痕迹中可见：

- `clean driver.exe`
- `clean game file.exe`
- `Fix-LCU-Window.exe`
- 多个驱动、安全组件和目录名称
- 网络库、URL 与外部站点字符串

这些结果说明旧版不仅是一个简单界面程序。未签名会增加 Windows 警告，但内嵌多个可执行文件、运行时释放程序、受保护目录操作和网络相关代码，同样会显著提高安全软件告警概率。

## FACM 2.1 处理方式

1. UI 重写为 64×64 桌面悬浮球，不再使用旧版大窗口代码。
2. 不联网，不包含自动更新器、遥测或远程配置。
3. 不保留涉及驱动、安全组件移除或批量游戏目录删除的旧工具。
4. 保留 `Fix-LCU-Window.exe` 1.1.2 及四种运行模式；资源压缩嵌入，释放后必须通过固定 SHA-256 校验。
5. 清理范围限制为客户端顶层日志和 FACM 自身临时文件。
6. 构建可复现；正式签名仅在发布流水线最后一步执行。
